[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["cd functions && pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "cd functions && gunicorn app:app --bind 0.0.0.0:$PORT"
