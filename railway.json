{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "cd functions && gunicorn app:app --bind 0.0.0.0:$PORT",
        "healthcheckPath": "/",
        "healthcheckTimeout": 100
    }
}